omdb.settings:
  type: config_object
  label: 'OMDb settings'
  mapping:
    api:
      type: string
      label: 'Link to api search movies:'
    poster_source:
      type: string
      label: 'Link to poster image source'
    api_url:
      type: string
      label: 'Other part ot url'
    line:
      type: integer
      label: 'Set lines'
